rate-limiter

A lightweight TypeScript implementation of the Token Bucket rate limiting algorithm.

Features
- configurable bucket size
- configurable refill rate
- simple request check API

Algorithm
Token Bucket